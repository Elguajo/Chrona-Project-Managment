# Next Session

Status: Phases 03–06 are complete. A local Microsoft Edge browser run through Playwright Core used only isolated temporary SQLite databases with realistic fixtures and confirmed native HTML5 Kanban `dragstart`/`drop` reorder and cross-column moves across reload; `Move to…` completion/reopening and lifecycle persistence; Timeline middle-button pan, Space + left-drag, cursor-preserving ordinary-wheel zoom, and dynamic range growth at both edges; and every List sortable column in both directions. The run found and fixed Kanban ordering of initially unordered cards, a passive Timeline wheel listener, the missing app icon request, backup-import failure announcement, and mobile page overflow. The approved Project workspace expansion has added durable Tasks, Milestones, and Documents as Project-owned relations; it does not add collaboration, AI/MCP, CRM, or cloud synchronization.

Current phase: Phase 07 — Personal Workflow Polish (`.progressive/phases/07-personal-workflow-polish.md`). Global keyboard navigation remains future scope; `⌘/Ctrl+K` is intentionally limited to creation commands.

Read first: Project Brief → Architecture → Roadmap → current phase → ADR-002. Use `CONTEXT_MANIFEST.json` for non-default hints. Consult `docs/source/LOCAL_PROJECT_OS_FINAL_SPEC_v2.md` only when a requirement is unclear; it is source material, not default context.

Reference evidence is documented in `docs/references/`. It may influence visual implementation only after the note is considered; it cannot silently change scope.

Tooling profile: Recommended, with native fallbacks. Serena and GitHub Spec Kit were detected locally; no installation/configuration has occurred.

Verified foundation commands: `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm db:migrate`, and a production runtime smoke check. `README.md` contains local run and validation instructions.

Verified Phase 02 commands: `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm db:migrate`, and a production runtime smoke request. The domain migration is `drizzle/0001_wandering_smiling_tiger.sql`.

Verified Phase 03 commands: `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, a temporary `pnpm db:migrate`, and a production HTTP smoke. Local Microsoft Edge browser QA through Playwright Core observed native HTML5 `dragstart`/`drop` reorder and cross-column status movement, `Move to…` completion/reopening, lifecycle persistence, search/priority/type filtering, and reload persistence on an isolated temporary SQLite database. No browser was downloaded.

Verified Project workspace expansion: `pnpm db:generate` created `drizzle/0002_sticky_redwing.sql`; `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, and `pnpm build` passed. The domain test covers Task completion, Milestone completion, local Document editing, ownership rejection, direct child deletion, and project cascading deletion.

Verified Timeline and Milestones: `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, a fresh temporary `pnpm db:migrate`, and production HTTP smoke passed after the continuous viewport update. Deterministic coverage includes scale clamping, cursor-preserving zoom, adaptive tick thresholds, fit/navigation, yearly range extension, clipping, and overdue semantics. Browser QA covered Day/Week/Month/Quarter density, controls, filters, Today/overdue markers, drawer Escape/focus return, cross-view viewport retention on 509 realistic Projects, and in local Microsoft Edge: middle-button and Space-drag panning, cursor-preserving wheel zoom, and both dynamic range extensions. No browser console errors occurred.

Verified List and Dashboard: `pnpm typecheck`, `pnpm lint`, `pnpm test:domain`, and `pnpm build` passed. A production server backed by a temporary SQLite database rendered Dashboard, Project, Task, and Milestone content in its initial HTML response; the process was stopped after the smoke. Local Microsoft Edge browser QA verified every List sortable column in both directions against the same persisted Project fixtures used by Kanban and Timeline.

Verified standalone Project workspace: `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, and `pnpm build` passed. A production server backed by a fresh temporary SQLite database rendered `/`, a populated `/projects/[id]` workspace (including Document content), and the missing-project 404; the process was stopped after the smoke. The route reads the existing Project aggregate plus its activity and server actions revalidate both route surfaces.

Verified Templates and Quick Add: migration `0003_shallow_donald_blake` creates three immutable starter templates and relational template children. Personal templates support CRUD; Project creation expands a template in one SQLite transaction and calculates dates from the required start date. The header Quick Add button and `⌘/Ctrl+K` expose create-only Project, Task, Milestone, and Document flows. `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, and `pnpm build` passed; a production HTTP smoke rendered the seeded template and Quick Add labels from a fresh temporary SQLite database.

Verified Calendar and Agenda browser QA: an isolated local database with 18 fixtures exercised Month and Week, previous/next and Today, record-type/status filters, search, 15-entry day expansion, and Project drawer open/close focus return. `2026-08-31` stayed in the August boundary cell while `2026-09-01` Task and Milestone entries stayed on September 1 across Month and Week. Opening the drawer originally logged React's server-action form `encType` warning; removing the redundant attribute from the shared Project form eliminated the error in a fresh browser session. `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, temporary `pnpm db:migrate`, and production HTTP smoke passed.

Verified Phase 06: versioned local JSON backup v1 includes all durable SQLite tables, settings, and local cover assets. Import validates untrusted JSON fully before it stages cover files and transactionally replaces SQLite data; domain tests confirmed successful restore and rejection of incompatible or broken-relation imports without overwriting the prior data. Local Microsoft Edge browser QA on an isolated 500-Project database confirmed the native `Move to…` focusable alternative, Kanban-to-List synchronization, export/change/restore, assertive invalid-import feedback, only local HTTP requests, and responsive clipping of page overflow at 390px while Kanban retains its own scroller. Dashboard initial render measured 196 ms and Timeline switch 158 ms; virtualization was not warranted. `pnpm test:domain`, `pnpm check:database`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, a fresh temporary `pnpm db:migrate`, and a production HTTP smoke for `/` and `/api/backup` passed.

Next implementation action: begin Phase 07 from its acceptance criteria. Keep established boundaries intact: one local SQLite database, one Project portfolio source of truth, Project-owned workspace records, no auth, no remote services, and no collaboration/AI/MCP/CRM/cloud sync.
